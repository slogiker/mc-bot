1. free secure tunnell via playit.gg

2. when selecting modded platform like paper or fabric, allow user to write name of the mod in text field, you can write as many as you want, if you type 0 you go out of mod selectin, after all mods are given use coresponing api to check their database for ilike name and same version for that mod, before starting server install all mods and check dependencies

3. option to add vanilla tweaks zip folder, unzip it and use it for current world

4. allow user to choose version from drop down menu 

5. make settings in one form with one main continue at the bottom, if server is not on whitelist add wont work so lets fix that

6. bot automaticly creates channels:
   Server information - IP, Version, Seed, spawn typed manualy
   bot-commands - only channel bot listens
   logs

7. automaticly allocate max and min ram when starting minecraft server jar instance
